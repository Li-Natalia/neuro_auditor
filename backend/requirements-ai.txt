# ===== Heavy AI/ML dependencies (optional) =====
# Install only when enabling the local NLP/embeddings stack:
#   pip install -r requirements-ai.txt
#
# The app runs fully without these (rule-based chat fallback is used).
# Note: the YandexGPT/OpenAI chatbot needs only `openai`, which ships in
# requirements.txt — you do NOT need this heavy stack to use the LLM chatbot.

torch==2.3.1
transformers==4.41.2
spacy==3.7.4
scikit-learn==1.5.0
numpy==1.26.4
